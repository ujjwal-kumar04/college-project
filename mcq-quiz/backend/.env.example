# Production Environment Variables for Render.com
# Add these in your Render.com dashboard under Environment Variables

# MongoDB URI (replace with your MongoDB Atlas connection string)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/mcqquiz?retryWrites=true&w=majority

# JWT Secret (generate a strong secret for production)
JWT_SECRET=your_production_jwt_secret_key_minimum_32_characters_long

# Server Port (Render automatically sets this, but you can specify)
PORT=5001

# Node Environment
NODE_ENV=production

# CORS Origins (add your frontend URLs)
ALLOWED_ORIGINS=https://college-project-roan.vercel.app,https://your-custom-domain.com